# === Core Framework ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# === Database ===
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pgvector>=0.3.6
alembic==1.14.1

# === Data & ML ===
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.1
onnxruntime==1.20.1
skl2onnx==0.17.0
xgboost==2.1.3
lightgbm==4.5.0
shap==0.46.0

# === NBA Data ===
nba_api==1.10.2

# === Visualization (EDA) ===
matplotlib==3.10.0
seaborn==0.13.2
jupyter==1.1.1

# === RAG / Intelligence (Phase 4) ===
google-generativeai==0.8.4
chromadb==0.5.23
langchain==0.2.16
langgraph==0.2.28

# === LangChain Enhancements (Phase 6.1 — SCR-317) ===
langchain-google-genai>=2.0.0    # Gemini integration via LangChain interface
langchain-community>=0.2.0       # Community tools, SQL loaders, memory helpers

# === PyTorch Neural Networks (Phase 6.2 — SCR-318) ===
torch>=2.2.0                     # MLP model + entity embeddings + LSTM (Module 6.5)
tensorboard>=2.16.0              # Training curve visualisation

# === Statistics Module (Phase 6.4 — SCR-320) ===
statsmodels>=0.14.0              # SPRT p-values, OLS, time-series tests
# pymc>=5.10.0                   # Full Bayesian (heavy dep — install separately for local use)
# arviz>=0.17.0                  # Bayesian posterior visualisation (paired with PyMC)

# === Time-Series Forecasting (Phase 6.5 — SCR-324) ===
prophet>=1.1.5                   # Additive time-series model with changepoint detection
ruptures>=1.1.0                  # Offline changepoint detection (Pelt, Binseg algorithms)

# === LLM Fine-tuning (Phase 6.6 — SCR-325) ===
# Install manually for local fine-tuning (heavy deps, not needed for serving):
# pip install peft>=0.9.0 trl>=0.7.0 bitsandbytes>=0.42.0 accelerate>=0.27.0 evaluate>=0.4.0 transformers>=4.38.0 datasets>=2.18.0
# peft>=0.9.0              # LoRA/PEFT adapter training
# trl>=0.7.0               # SFTTrainer for supervised fine-tuning
# bitsandbytes>=0.42.0     # 4-bit quantisation (QLoRA)
# accelerate>=0.27.0       # Multi-GPU + mixed precision training
# evaluate>=0.4.0          # ROUGE, accuracy metrics
# transformers>=4.38.0     # HuggingFace model loading (Phi-3-mini)
# datasets>=2.18.0         # HuggingFace dataset format

# === Scheduler ===
apscheduler>=3.10.0

# === Observability ===
langfuse>=3.7.0
prometheus-fastapi-instrumentator==7.0.0
slowapi==0.1.9

# === Utilities ===
python-dotenv==1.0.1
pyyaml==6.0.2
httpx==0.28.1
requests==2.32.3
urllib3<2  # Keep compatibility with local LibreSSL-linked Python runtimes

# === Testing ===
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
